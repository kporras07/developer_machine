---
- hosts: all
  become: yes

  vars_files:
    - vars/main.yml
    - ../config.yml

  gather_facts: False
  pre_tasks:
    - include: tasks/init.yml
    - include: tasks/init-debian.yml

  roles:
    # Essential roles.
    - geerlingguy.git
    - dwcramer.ansible
    - andrewrothstein.vagrant
    - gvillalta99.nvm
    - altermn.rvm
    - geerlingguy.drush
    - { role: trozz.atom, when: install_atom == true }
